# Architecture

The browser calls only the public catalog contract with a publishable key. Synthetic fixtures are the default until an approved sandbox URL is configured. Cart and checkout mutations are intentionally absent until their platform contracts and idempotency rules ship.
